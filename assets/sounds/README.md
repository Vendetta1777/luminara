# sounds/

Placeholder. Luminara's audio is **synthesized at runtime** via the Web Audio API
(see `src/audio.js`), so there are no sound files here by design.

This folder exists for any future pre-recorded audio (e.g. a music track).
