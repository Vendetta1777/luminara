// ui.js — the HUD and menus.
// Owns everything drawn on top of the game: evolution name, progress bar,
// tutorial hint, and the depth score. Isolated with ctx.save()/restore()
// so UI rendering never bleeds into the game world.
//
// Milestone 7 implements the UI class.
