// main.js — bootstrap.
// The ONLY file that touches the DOM directly: creates the canvas context,
// builds the Game, wires input, and starts the loop. Kept intentionally thin.
//
// Milestone 1: just confirm the module wiring works. The real canvas/loop
// setup arrives in Milestone 2.

console.log('Luminara — scaffolding loaded.');
