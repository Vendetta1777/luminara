// game.js — the conductor.
// Owns the requestAnimationFrame loop and orchestrates every system
// (world, particles, player, ui) in the correct draw order each frame.
//
// Milestone 2 implements the Game class with start(), stop(), and loop().
